{
  "name": "electron-test",
  "version": "1.0.10",
  "description": "",
  "main": "main.js",
  "dependencies": {
    "electron-updater": "^4.3.1",
    "update-electron-app": "^1.5.0"
  },
  "devDependencies": {
    "electron": "^11.5.0",
    "electron-builder": "^22.7.0"
  },
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/20matan/electron-autoupdate-test.git"
  },
  "author": "",
  "license": "ISC"
}
